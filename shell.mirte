includes:
  - socketServer
definitions:
  shellServer:
    implementedBy: sarah.shell.ShellServer
    inherits: [ unixSocketServer ]
    settings:
      manager: {type: manager}
      socketPath: {default: shell.socket}
